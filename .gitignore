node_modules/
dist/
client/src/**/*.js
server/data/*.db
server/data/*.db-*
*.log
.DS_Store
.env

# Python backend
.venv/
__pycache__/
*.py[cod]
.pytest_cache/

# Built client copied into the image; never committed
server/static/
build/

# 41MB 的 CC0 家具模型。不進 repo——沒有它們 app 照常跑（家具退回程式生成的
# 幾何、面板退回畫出來的圖例），要的人自己 `npm run assets`。
# 貼圖（client/public/textures，10MB）留著：那是牆、地板、每一個表面都要用的，
# 沒有它整個場景就是素色，不存在「有需要的人」這種區分。
client/public/models/

# 素材抓取的中繼檔（Quaternius 的 OBJ 原始檔）
.cache/

# 打包出來的 Windows 安裝程式（96MB）。跟 dist/ 一樣是建置產物，
# 不是原始碼——`packaging/installer.iss` 才是。
packaging/out/
